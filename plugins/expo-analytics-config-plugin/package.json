{
  "name": "expo-analytics-config-plugin",
  "version": "0.1.2",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "build",
    "src"
  ],
  "license": "MIT",
  "peerDependencies": {
    "@expo/config-plugins": "*"
  },
  "devDependencies": {
    "@expo/config-plugins": "^8.0.0",
    "typescript": "^5.4.0",
    "rimraf": "^6.0.1"
  },
  "scripts": {
    "build": "tsc -p .",
    "clean": "rimraf ./build || true",
    "prepare": "npm run build"
  }
}